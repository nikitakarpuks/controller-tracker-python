  Option A — Far translation instead of scale (minimal change)

  Replace the scale attempt with a large translation, and drop the recursive parent Clear (it likely fights the child transform):

  # Replace the hide block at line 727-732 with:
  for ctrl_name in self._controllers_vis:
      if ctrl_name not in T_cam_model_per_ctrl:
          # Move mesh off-screen instead of scaling — more reliable
          rr.log(f"world/{ctrl_name}/mesh",
                 rr.Transform3D(translation=[0.0, 0.0, -99999.0]))
          # Clear per-frame per-controller components individually
          for _sub in ("leds", "normals"):
              rr.log(f"world/{ctrl_name}/{_sub}", rr.Clear(recursive=False))
          for _ci in self._cameras:
              for _sub in ("rays", "projected_leds", "led_ids", "errors", "error_values"):
                  rr.log(f"world/{ctrl_name}/camera_{_ci}/{_sub}", rr.Clear(recursive=False))

  Pros: Tiny diff, preserves static mesh. Cons: Mesh sits at -99999m, which is ugly if you ever look at the whole scene.

  ---
  Option B — Drop static=True, log mesh only on visible frames

  Remove static=True and move the mesh log into the per-controller loop. The mesh is only logged once on the first frame it appears and whenever it
  reappears, making the rrd overhead roughly proportional to the number of controller-on transitions (not total frames):

  # In start(): remove the static mesh block entirely (lines 503-514)

  # In _log_frame(), inside the per-controller loop, add mesh logging:
  if self.vis_cfg.get("show_mesh", True):
      rr.log(f"{ctrl_path}/mesh",
             rr.Mesh3D(
                 vertex_positions=self._mesh_verts[ctrl_name],
                 triangle_indices=self._mesh_faces,
                 vertex_normals=self._mesh_vertex_normals,
                 albedo_factor=[0.7, 0.7, 0.7, 1.0],
             ))
      rr.log(f"{ctrl_path}/mesh",
             rr.Transform3D(translation=T4[:3, 3], mat3x3=T4[:3, :3]))

  # In the hide block: just log Clear on the mesh entity
  rr.log(f"world/{ctrl_name}/mesh", rr.Clear(recursive=False))

  To limit repeated mesh geometry logging, you can track the previous visibility state and only re-log geometry on transitions (not every frame).

  Pros: Correct and simple. Cons: Each reappearance re-logs 3D mesh geometry — depending on mesh size and transition frequency this may increase
  file size noticeably.

  ---
  Option C — Separate pose parent entity (cleanest architecture)

  Keep mesh static, but put pose on a dedicated parent entity instead of on the mesh entity itself:

  # Static mesh at start() — same as now but no transform on this entity
  rr.log(f"world/{ctrl_name}/body/mesh", rr.Mesh3D(...), static=True)

  # Per frame, visible: log pose on the parent
  rr.log(f"{ctrl_path}/body",
         rr.Transform3D(translation=T4[:3, 3], mat3x3=T4[:3, :3]))

  # Per frame, invisible: move the parent off-screen
  rr.log(f"world/{ctrl_name}/body",
         rr.Transform3D(translation=[0.0, 0.0, -99999.0]))

  This cleanly separates static geometry from per-frame pose. The transform on the parent propagates to all children (mesh, leds, etc.)
  automatically.

  Pros: Clean, reliable, static mesh file-size benefit is preserved. Cons: Requires renaming world/{ctrl_name}/mesh → world/{ctrl_name}/body/mesh
  throughout, and the other per-frame components (leds, rays) that are currently logged in world space would need to be restructured too, otherwise
  they'd get the transform applied twice.

  ---
  My recommendation: Start with Option A — it's a 5-line change and the -99999m position won't matter in practice since the camera views are focused
  close to the scene. If file size is critical and transitions are rare, Option B with change-tracking is the right long-term answer.









  The parallelization plan is clean — faster result becomes the occluder, slower gets the mask, current-frame guard is satisfied by construction. No
  design change needed when you parallelize, just the invocation order changes.

  Here is the full refined plan.

  ---
  Cross-Controller LED Occlusion — Full Implementation Plan

  ---
  Step 1 — config.yml: Three new keys

  Under the matching section:

  matching:
    cross_controller_occlusion: false         # master switch, opt-in
    cross_occlusion_bounding_radius_m: 0.18  # manually tuned controller bounding sphere
    cross_occlusion_gate_margin_px: 20.0     # slack added to the 2D gate threshold

  Bounding radius is user-defined — primitive stacking makes computed values unreliable, and a single hand-tuned number per controller type is
  simpler and more accurate.

  ---
  Step 2 — _visibility.py: Two geometry-transform helpers

  Placed alongside the existing _rays_blocked_by_* functions:

  _box_to_cam(box: Box3D, R: np.ndarray, t: np.ndarray) -> Box3D
  Returns a new Box3D instance. center_new = R @ box.center + t, axes_new = R @ axes_matrix (each column rotated), half_dims unchanged.

  _cylinder_to_cam(cy: Cylinder3D, R: np.ndarray, t: np.ndarray) -> Cylinder3D
  Returns a new Cylinder3D instance. center_new = R @ cy.center + t, axis_new = R @ cy.axis, all scalar fields unchanged.

  Both return new instances — originals are never mutated, since the same geometry objects are reused every frame for self-occlusion checks.

  ---
  Step 3 — _visibility.py: 2D projection gate

  _occlusion_gate(tvec_A, tvec_B, br_A, br_B, focal_px, margin_px) -> bool

  Projects both controller origins with simplified pinhole (x_proj = tvec[0]/tvec[2] * focal). Returns True (gate open, run the full check) when:

  dist_2d(proj_A, proj_B)  <  focal * (br_A / tvec_A[2]  +  br_B / tvec_B[2])  +  margin_px

  Hard-return False immediately if either tvec[2] <= 0. Pinhole projection is fine here — the gate is intentionally conservative and margin_px
  absorbs any distortion-induced error.

  ---
  Step 4 — _visibility.py: _cross_occluded_mask

  def _cross_occluded_mask(
      R_B, tvec_B,       # T_cam_B: B's current predicted pose in this camera
      positions_B,       # (N, 3) B's LED positions in controller-B frame
      R_A, tvec_A,       # T_cam_A: A's current-frame pose in this camera
      geom_A,            # ControllerGeometry of the occluder (controller A)
      br_A, br_B,        # bounding radii for gate
      focal_px,
      gate_margin_px,
  ) -> np.ndarray        # bool (N,), True = LED occluded by A's body

  Execution order:

  1. Call _occlusion_gate → return np.zeros(N, bool) immediately if gate fails.
  2. Transform A's primitives to camera frame using _box_to_cam / _cylinder_to_cam with R_A, tvec_A.
  3. Transform B's LEDs to camera frame: led_cam_B = (R_B @ positions_B.T).T + tvec_B.
  4. cam_origin = np.zeros(3).
  5. blocked = np.zeros(N, bool). Iterate A's transformed boxes and cylinders, OR-accumulate: blocked |= _rays_blocked_by_box(cam_origin, led_cam_B,
  box) / _rays_blocked_by_cylinder(...).
  6. Return blocked.

  Zero new intersection mathematics — entirely reuses the existing vectorized functions.

  ---
  Step 5 — _matching.py: New parameter + injection at visibility-mask call sites

  New parameter added to proximity_match and brute_match:

  occluders_per_cam: Optional[Dict[int, Tuple[np.ndarray, np.ndarray, ControllerGeometry]]] = None
  # cam_idx → (R_occ_in_cam, tvec_occ_in_cam, geom_occ)

  br_A, br_B, gate_margin_px are read from _cfg (already the pattern for all config inside these functions). focal_px is already computed inside
  both.

  Injection pattern — applied after every _visible_mask(...) call:

  vis_mask = _visible_mask(R, tvec, ...)
  if occluders_per_cam:
      _occ = occluders_per_cam.get(cam_idx)
      if _occ is not None:
          _R_occ, _t_occ, _geom_occ = _occ
          vis_mask &= ~_cross_occluded_mask(
              R, tvec, self.model.positions,
              _R_occ, _t_occ, _geom_occ,
              br_A, br_B, focal_px, gate_margin_px,
          )

  Three call sites to update:

  ┌───────────────────────────────────────────────────────────────┬────────────────────────┐
  │                           Location                            │     cam_idx to use     │
  ├───────────────────────────────────────────────────────────────┼────────────────────────┤
  │ proximity_match — main vis_mask_pred                          │ self.camera.camera_idx │
  ├───────────────────────────────────────────────────────────────┼────────────────────────┤
  │ proximity_match — aux expansion loop _vis_i_r                 │ _ocam.camera_idx       │
  ├───────────────────────────────────────────────────────────────┼────────────────────────┤
  │ brute_match — initial visibility before LED triple generation │ self.camera.camera_idx │
  └───────────────────────────────────────────────────────────────┴────────────────────────┘

  ControllerTracker.track() is also updated to accept occluders_per_cam and forward it unchanged to both proximity_match and brute_match.

  ---
  Step 6 — controller.py: Guard, build, and pass the occluder

  Injection point: in the per-controller ordered loop, immediately before calling primary_tracker.track() for controller B.

  Guard chain — all conditions must hold, evaluated in order:

  1. bool(_cfg.get('cross_controller_occlusion', False)) — feature enabled
  2. At least two controllers are being processed this frame
  3. Controller A has a solution from the current frame — solution_A is present in this frame's results dict, not a prev_pose carry-over. This is
  the freshness guard: a stale occluder pose during fast motion is worse than no occlusion mask at all.
  4. solution_A["T_world_ctrl"] is present.

  If any guard fails → pass occluders_per_cam=None.

  Building the dict when guards pass:

  T_world_A = solution_A["T_world_ctrl"]
  _br    = float(_cfg.get('cross_occlusion_bounding_radius_m', 0.18))
  _geom_A = <controller A's ControllerGeometry object>

  occluders_per_cam = {}
  for _cam_id, _cam in self.cameras.items():
      _T_cam_A = _cam.T_world_cam.inverse().compose(T_world_A)
      occluders_per_cam[_cam_id] = (
          _T_cam_A.R.astype(np.float32),
          _T_cam_A.t.astype(np.float32),
          _geom_A,
      )

  Parallelization note (future): when A and B run in parallel, the faster result becomes the occluder; the slower one receives the dict. The
  current-frame guard is satisfied by construction — no design change required, only the invocation pattern changes.

  ---
  Step 7 — Validation in Rerun

  No algorithmic unit tests needed — the existing pipeline tests cover the modified code paths. Manual validation:

  1. Enable cross_controller_occlusion: true, run a sequence with controllers overlapping.
  2. In Rerun, LEDs of B behind A's body should appear as not visible (no projected disk, no ray) rather than unmatched (orange ray to empty
  frustum). That's the visible signal the feature is working.
  3. Run a standard non-overlapping sequence and confirm no regression in match counts or error.
  4. Tune cross_occlusion_bounding_radius_m until the gate fires at a sensible distance and cross_occlusion_gate_margin_px until no spurious misses
  occur near the boundary.

  ---
  Summary

  ┌────────────────┬──────────────────────────────────────────────────────────────────────────┬────────┐
  │      File      │                                  Change                                  │ ~Lines │
  ├────────────────┼──────────────────────────────────────────────────────────────────────────┼────────┤
  │ config.yml     │ 3 new keys                                                               │ 3      │
  ├────────────────┼──────────────────────────────────────────────────────────────────────────┼────────┤
  │ _visibility.py │ 2 transform helpers + gate + _cross_occluded_mask                        │ ~65    │
  ├────────────────┼──────────────────────────────────────────────────────────────────────────┼────────┤
  │ _matching.py   │ occluders_per_cam param + injection at 3 call sites + forward in track() │ ~30    │
  ├────────────────┼──────────────────────────────────────────────────────────────────────────┼────────┤
  │ controller.py  │ Guard + build dict + pass through                                        │ ~35    │
  └────────────────┴──────────────────────────────────────────────────────────────────────────┴────────┘

  Total: ~133 lines. Zero new algorithms. Zero new dependencies.




















# Session Summary — Pose-Guided Threshold System
    2
    3 ## What we did
    4
    5 ### 1. Code cleanup in `blobs_detection.py`
    6
    7 **Fixed `min_area` bug (critical)**
    8 - `cv2.contourArea` computes the geometric Shoelace area, not pixel count. For small
    9   irregular blobs (e.g. a 3-pixel L-shape) this returns ~0.5, causing valid blobs to
   10   be rejected even with `min_area: 3`.
   11 - Fix: build the ROI pixel mask first, then check `len(ys_roi) < min_area` (pixel count).
   12 - Max-area check still uses `cv2.contourArea` — accurate for larger blobs.
   13
   14 **Removed dead/redundant filters**
   15 - `max_wh` check — redundant with `max_area` + circularity filter.
   16 - Area outlier filter (MAD-based, step 3b) — redundant once `max_area` is well-calibrated
   17   (which pose-guided detection will ensure).
   18 - Gradient smoothness filter (step 3d, ray casting) — almost never fired; removed entirely.
   19
   20 **H1 ("deep in large blob") color changed to light blue**
   21 - H1 is only active in the 2-pass cold-start path. It will be inactive in the future
   22   pose-guided single-pass path (no pass-1 large blobs to check against).
   23 - Color change signals "no-prior path only" in visualisation.
   24
   25 ### 2. `main.py` — selected cameras guard
   26 Added `if cam_idx not in cameras: continue` in the blob detection loop.
   27 `cameras` dict is the authoritative source (built from `selected_cameras` config key).
   28 Prevents any non-selected camera from running detection or saving visualisation files.
   29
   30 ### 3. Calibration data collection infrastructure
   31
   32 **`blobs_detection.py` — `get_pass2_params(cam_idx)`**
   33 - New public function that reads `_pass2_memory` for the given camera.
   34 - Returns `{pixel_threshold, required_threshold, max_area}` or `None` if pass-2 didn't run.
   35
   36 **`config.yml` — `debug.calibration_csv`**
   37 - New opt-in key (default `null`). Set to a file path to enable CSV logging.
   38
   39 **`main.py` — CSV writing**
   40 - After each `tracking_system.update()`, for each successful solution:
   41   - `depth_m = sol["tvec"].reshape(3)[2]` (ring-center depth, origin is LED ring centroid)
   42   - Calls `get_pass2_params(primary_cam_idx)`
   43   - Writes one row if pass-2 ran
   44 - CSV columns: `frame, ctrl_name, cam_idx, depth_m, pixel_threshold, required_threshold, max_area`
   45 - File opened before loop, closed and logged after.
   46
   47 ### 4. `data/filter_csv.py` — calibration analysis tool
   48
   49 Offline script to process raw CSV into a clean calibration curve:
   50
   51 1. Load + drop NaN rows
   52 2. Sort by `depth_m`
   53 3. Rolling-window outlier removal (local median ± `OUTLIER_K × MAD`, window=50)
   54 4. Equal-count quantile binning (50 bins) → median per bin
   55 5. Save filtered CSV
   56 6. Fit both `a/d + b` and `a/d² + b` for each column; print R² for each
   57 7. Print config-ready YAML block with best model + coefficients
   58 8. 2×3 matplotlib figure:
   59    - Top row: depth vs each threshold column, both fitted curves + R² in legend
   60    - Bottom row: linearisation space (`1/d` for thresholds, `1/d²` for max_area)
   61
   62 **Key finding from data analysis:**
   63 - Controllers are symmetrical — data from both can be pooled.
   64 - Model origin (0,0,0) is the LED ring centroid; `tvec[2]` is an excellent depth proxy
   65   (LED z-spread is < ±1 cm, negligible at tracking distances).
   66 - Current raw CSV covers 0.07–0.3 m. This range is **insufficient** — extrapolation to
   67   1 m (3.3× beyond calibration range) is unreliable due to model uncertainty (`1/d` vs
   68   `1/d²` diverge substantially at that distance).
   69 - Fitted parametric curve is better than bin-median interpolation: bin medians are
   70   integer-quantized and noisy; the curve averages all data and is physically grounded.
   71
   72 ---
   73
   74 ## What needs to be done
   75
   76 ### Step 1 — Collect calibration data across full depth range
   77
   78 - Run with `debug.calibration_csv` set and `pass2_threshold_factor > 0`
   79 - Use **single-controller datasets only** (two controllers contaminate the adaptive
   80   thresholds)
   81 - Dataset must cover **0.07 m to at least 1.5 m** with continuous movement
   82   (slowly move controller from close to far and back)
   83 - Aim: meaningful coverage at 5–6 distinct depth levels across the full range
   84 - Orientation: tilt controller so inner LEDs are just visible (worst-case brightness
   85   scenario for threshold calibration)
   86
   87 ### Step 2 — Run `filter_csv.py` and choose model
   88
   89 - Run on the new full-range CSV
   90 - Check the bottom-row linearisation plots:
   91   - If `pixel_threshold` vs `1/d` is a straight line → use `a/d + b`
   92   - If `max_area` vs `1/d²` is a straight line → use `a/d² + b`
   93 - Note R² values for each column and model
   94 - Paste the printed config block into `config.yml` under `pose_guided_thresholds`
   95
   96 ### Step 3 — Implement pose-guided detection in `blobs_detection.py`
   97
   98 - `get_centroids` accepts new parameter `predicted_depth: float | None`
   99 - When `predicted_depth` is provided:
  100   - Compute `pixel_threshold` and `required_threshold` from fitted model coefficients
  101     (`cfg["pose_guided_thresholds"]`)
  102   - Compute `max_area` from its fitted model
  103   - Call `_detect_blobs` **once** with these values (skip the entire 2-pass adaptive system)
  104 - When `predicted_depth` is `None` (cold start): current 2-pass behavior unchanged
  105 - No EMA, no count gate, no percentile logic when depth is known
  106
  107 ### Step 4 — Expose predicted depth from `TrackingSystem`
  108
  109 Add `get_predicted_depths_per_camera() -> Dict[int, List[float | None]]` to `TrackingSystem`
  110 in `controller.py`:
  111 - Iterate `self.trackers`; for each `(ctrl_name, cam_id)` with `prev_pose` set:
  112   - Extract `tvec[2]` as depth (or median LED depth for higher accuracy)
  113 - Return `{cam_id: [depth_ctrl0, depth_ctrl1, ...]}` ordered by ctrl_names sorted
  114
  115 ### Step 5 — Wire into `main.py`
  116
  117 Before the inner camera loop each frame:
  118 ```python
  119 depth_hints = tracking_system.get_predicted_depths_per_camera()
  120 ```
  121
  122 For each controller, call `get_centroids` with the matching depth hint:
  123 ```python
  124 blob_centroids, ... = get_centroids(
  125     image, config["blob_detection"], ...,
  126     predicted_depth=depth_hints.get(cam_idx, [None])[ctrl_idx],
  127 )
  128 ```
  129
  130 Note: in future this becomes one `get_centroids` call per controller (parallel),
  131 each with its own depth-derived thresholds. For now, a single call per camera
  132 with the depth of the controller that is currently tracked (or None).
  133
  134 ### Step 6 — Validate
  135
  136 - Compare blob detection visualisations (pass-1 vs pose-guided single-pass)
  137 - Check that inner LEDs at far range are still detected with pose-guided thresholds
  138 - Confirm no regression for close-range detection
  139 - Verify that `pixel_threshold` and `required_threshold` in pose-guided mode are
  140   always >= base `min_threshold` / `required_threshold` config values (add clamp)
  141
  142 ---
  143
  142 ---
  143
  144 ## Open decisions
  145
  146 | Topic | Decision needed |
  147 |---|---|
  148 | One `get_centroids` call per camera (current) vs one per controller | For now: one per camera, pass depth of primary tracked controller |
  149 | Fallback when pose-guided threshold < base config threshold | Clamp to `max(pose_guided, base_config)` |
  150 | `max_area` model: `a/d+b` vs `a/d²+b` | Determine from Step 2 R² values |
  151 | How to handle frame where one controller is tracked, other is cold | Pose-guided for tracked one; 2-pass adaptive for cold one — needs th
      ought |
















      I think passing large areas and handle all possible cases is the wrong way.
  So, answering your question, we now are talking about only the pose-based blob detection. Current 2 pass cold approach should stay the same.
  So, with warm case we have a prediction for our controller next position, this is the frame we are looking blobs in.
  Currently we use just the controller depth. What about extending this idea:
  1) so we have this pose prediction. We can easily check which leds are theoretically visible in this pose and


   great, it works.
  Now please think - we have 3 important thresholds for which the calculation is data-driven - max area, min and reuired thresholds.
  I want it now to be led-specific and BE DEPENDENT ON A PRATICULAR LED DISTANCE TO CAMERA AND ITS ANGLE TOWARDS IT.
  We have a calibration_csv idea when we calulate stats based on only whole controller depth.
  Issue is that we have algorithms that use general thresholds for whole image.
  Do you think that the only and rigth way is to theoretically assume how this dependency should be modelled and then adjsut params based on the real case? What are your thoughts?
  ⎿  ⧉ Selected 1 lines from ../config/config.yml in PyCharm

● Good question, worth thinking through carefully.

  Yes, theory + data fitting is the right approach, and here's why it's not just "the only way" but genuinely the correct engineering choice:

  The functional form is already known from physics:
  - Blob area ∝ cos(θ) / d² — projected solid angle of a tilted LED disk
  - Blob brightness ∝ cos(θ) / d² — Lambertian emission + inverse-square falloff

  Your existing model a/d² + b is already this, just without the cos(θ) factor (because you only had per-controller depth, so angle variation was averaged out). The extension is a *
  facing_cos / d² + b for each LED individually.

  Why not pure data-driven?
  You'd need a massive calibration dataset covering the full (d, θ) space, and the result would be a lookup table that doesn't generalize well to new camera positions or LED layouts.

  Why not pure theory?
  Real LEDs aren't ideal Lambertian emitters, camera sensors are non-linear, lenses have falloff. The a and b parameters absorb all that.

  What you already have:
  get_predicted_led_projections_per_camera already returns [proj_x, proj_y, depth_m, facing_cos] per visible LED. Both d and cos(θ) are right there. The calibration CSV just needs to
  record per-LED (not per-controller) observations: (d_i, facing_cos_i, brightness_i, area_i) during frames where tracking is confirmed good — then re-fit a * facing_cos / d² + b.

  The harder problem is the one you pointed out: blob detection currently runs globally with scalar thresholds before any LED assignment is known. The per-LED thresholds only become
  usable after you have a predicted pose. So realistically you end up with two regimes:
  1. Cold start / no pose: global thresholds as now
  2. Warm / pose available: per-LED local search with thresholds computed from predicted (d_i, facing_cos_i) — this is the pose-guided path you already partially have

  That's not a limitation, it's the natural architecture. Does this align with what you were thinking?





















  Both files share identical T_imu_cam for all cameras, and distortion is identical across files for cams 0, 2, 3. The only real difference is cam1 fx/fy (+1.1px) and cx/cy (~0.8px
  shift). Here's the full per-parameter risk breakdown:

  ---

  ┌────────┬────────────────────────────┬──────────────────────────────────────────────────────────────────────────────────────┬─────────────────────────────────┬──────────┐
  │ Param  │ Value range (across cams)  │                               Physical cause of change                               │           Drift risk            │ Priority │
  ├────────┼────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────┼──────────┤
  │ cx, cy │ 316–327 / 235–250 px       │ Lens shift on sensor from mechanical shock or nose-pad deformation from regular wear │ HIGH                            │ 1st      │
  ├────────┼────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────┼──────────┤
  │ fx, fy │ ~268–270 px                │ Thermal expansion of lens barrel during session warmup (~0.1–0.5%)                   │ MEDIUM                          │ 2nd      │
  ├────────┼────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────┼──────────┤
  │ p1, p2 │                            │ p1                                                                                   │ ≤0.0003,                        │ p2       │
  ├────────┼────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────┼──────────┤
  │ k1, k2 │ 0.43–0.76 / 0.25–0.66      │ Lens shape — glass doesn't change with use                                           │ NONE (but high impact if wrong) │ 4th      │
  ├────────┼────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────┼──────────┤
  │ k3–k6  │ small–large rational denom │ Same as k1/k2                                                                        │ NONE                            │ 5th      │
  ├────────┼────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────┼──────────┤
  ├────────┼────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────┼──────────┤
  │ rpmax  │ ~2.79 all cameras          │ Model boundary, not a physical quantity                                              │ NEVER                           │ —        │
  └────────┴────────────────────────────┴──────────────────────────────────────────────────────────────────────────────────────┴─────────────────────────────────┴──────────┘

  ---
  ├────────┼────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────┼──────────┤
  │ rpmax  │ ~2.79 all cameras          │ Model boundary, not a physical quantity                                              │ NEVER                           │ —        │
  └────────┴────────────────────────────┴──────────────────────────────────────────────────────────────────────────────────────┴─────────────────────────────────┴──────────┘
  ---
  Key observations from the data:  │ p1, p2 │                            │ p1                                                                                   │ ≤0.0003,                        │ p2       │
  ├────────┼────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────┼──────────┤
  │ k1, k2 │ 0.43–0.76 / 0.25–0.66      │ Lens shape — glass doesn't change with use                                           │ NONE (but high impact if wrong) │ 4th      │
  ├────────┼────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────┼──────────┤
  │ k3–k6  │ small–large rational denom │ Same as k1/k2                                                                        │ NONE                            │ 5th      │
  ├────────┼────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────┼──────────┤
  │ rpmax  │ ~2.79 all cameras          │ Model boundary, not a physical quantity                                              │ NEVER                           │ —        │
  └────────┴────────────────────────────┴──────────────────────────────────────────────────────────────────────────────────────┴─────────────────────────────────┴──────────┘

  └────────┴────────────────────────────┴──────────────────────────────────────────────────────────────────────────────────────┴─────────────────────────────────┴──────────┘

  ---
  Key observations from the data:

  1. cx/cy is already off-center: cam0 has cx=326.5 on a 640px-wide sensor (center would be ~320). That 6.5px offset is real optical decentering — any small lens shift amplifies into
  └────────┴────────────────────────────┴──────────────────────────────────────────────────────────────────────────────────────┴─────────────────────────────────┴──────────┘

  ---
  Key observations from the data:

  1. cx/cy is already off-center: cam0 has cx=326.5 on a 640px-wide sensor (center would be ~320). That 6.5px offset is real optical decentering — any small lens shift amplifies into
  significant projection error, especially for the high-distortion fisheye model you're using.
  2. k1 is huge (0.43–0.76) — this is fisheye-level. The distortion correction near image edges is enormous, so if k1 is wrong even by 1%, the edge error can be many pixels. But
  crucially: both calibration files agree on k1-k6 exactly for all cameras, so these are stable and likely correct from the factory.
  3. The only actual drift you observed between the two calibrations is cam1's fx/fy (+1.1px). That's thermal/mechanical and is already in your optimize_params.

  Recommendation: your current optimize_params: [fx, fy, cx, cy] is exactly right. The distortion coefficients shouldn't be added unless reprojection error stays high after cx/cy/fx/fy
  are tuned — and with sparse LED data you won't have enough coverage near image edges to reliably recover k1/k2 anyway.